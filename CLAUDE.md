# Darasa360 — Finance + Academics Monorepo

Two Laravel apps sharing a platform DB, deployed to a shared-hosting DirectAdmin/CloudLinux server. Solo-maintained — this file plus the two skills below (`deploy-to-live`, `debug-darasa`) are the persistent knowledge base across sessions. Read this before touching deployment, migrations, or server config.

**Never put real credentials, passwords, or API keys in this file or in the skills below — this repo is committed to git.** Credentials live in Claude's memory (`project_darasa_finance.md`, project type) and in each server's `.env` files, not here.

## Layout
- `finance/` and `academics/` — two independent Laravel apps, each with its own `composer.json`/`package.json`/`.env`
- Shared `platform` DB (schools registry, super admin, SSO tokens) — Finance owns the platform migrations (`finance/database/migrations/platform/`)
- Each app has a `tenant` DB connection for per-school databases, auto-provisioned via the DirectAdmin API (see `DA_*` env vars)
- Academics has a separate per-school migration path (`academics/database/migrations/school/`) applied via `php artisan migrate:schools` (see `app/Console/Commands/MigrateAllSchools.php`) — **not** picked up by the normal `php artisan migrate`
- Finance similarly has `finance/database/migrations/central/` for its own central-DB tables (`super_admins`, `schools`, `school_accountants`, `activity_logs`, `analytics_summary`) — run via `php artisan migrate --path=database/migrations/central`, also not picked up by the normal sweep
- Each school gets its **own dedicated MySQL user + password** (via `DirectAdminService::createDatabase()`, stored on the school record as `db_username`/`db_password`) — this host does not support granting one shared user access to arbitrary databases, so there is no shared-credential fallback that actually works for real schools (see "Resolved" section below for the full history of why)

## Branches
- `sandbox` — day-to-day development, deployed to `*-sandbox.darasa360.co.tz`
- `main` — production, deployed to `finance.darasa360.co.tz` / `academics.darasa360.co.tz`
- Workflow: commit to `sandbox` → push → `git checkout main && git merge sandbox --ff-only` → push `main` → pull on whichever live server folder(s) changed

## The #1 gotcha in this codebase
**The sandbox server sometimes has direct file edits that were never committed to git.** This has happened twice already: the entire Academics third-party template asset folder (`academics/resources/assets/vendor/`) and a full login-page redesign (`academics/resources/views/content/authentications/auth-login-basic.blade.php`) both existed only on the sandbox server's filesystem, invisible to `git log`/`git diff` on any machine, and were missed on the first live deploy because "it works on sandbox" wasn't actually backed by git.

**Before trusting that a fresh `git clone` + deploy will look/behave like sandbox, diff the sandbox server's actual files against git for anything user-facing** (blade views, public assets, config). `git status` run *on the sandbox server itself* will show these as modified/untracked — that's the tell. See the `deploy-to-live` skill for the exact commands.

## Server facts worth knowing up front
- SSH is on **port 22**. Port 2222 is the DirectAdmin control panel (HTTPS) — connecting SSH there fails at the banner-exchange stage and looks exactly like a firewall block, but isn't.
- This host's MySQL **default_storage_engine is MyISAM**, not InnoDB (unusual). `config/database.php` in both apps explicitly forces `'engine' => env('DB_ENGINE', 'InnoDB')` on every mysql connection — don't remove that, migrations with unique/long indexes will break with "max key length is 1000 bytes".
- `npm install` on this server is unreliable (CloudLinux/LVE process-limit `EAGAIN` errors from esbuild's postinstall spawning too many processes). Build frontend assets locally and upload `public/build/` via scp — don't run `npm run build` on the server itself.
- Git doesn't track empty directories: `bootstrap/cache` and several `storage/framework/*` subdirs won't exist after a fresh clone and must be created manually before `composer install`'s `package:discover` hook will succeed.
- Each domain's docroot must point at `<app>/public`, not the monorepo root — set via DirectAdmin's domain/subdomain table (a plain, user-editable "Docroot" column, not an admin-only setting).

## Fixed: Finance live .env had wrong DirectAdmin API variable names (2026-07-26)
Finance's `config/directadmin.php` reads `DA_HOST`/`DA_PORT`/`DA_USER`, but the live `.env` created during initial deployment used `DA_API_URL`/`DA_USERNAME` instead (copy-paste drift from whatever the original deployment notes had). Since `TenantDatabaseManager::createDatabase()` only calls the DirectAdmin API when `config('directadmin.user')` is non-empty, this silently made Finance's school-provisioning fall back to a raw-SQL path that doesn't work on this shared-hosting MySQL (same underlying constraint as the Academics issue below) — every new school created via Finance's admin panel on live would likely have hit this fallback. Fixed by correcting the live `.env` var names to match sandbox's (which was already correct) and re-caching config. **When creating any new environment's `.env` from a template, diff the variable names against what `config/directadmin.php` (or equivalent) actually reads — don't assume the template itself is correct.**

## Known incomplete feature: Academics→Finance super-admin cross-jump (found 2026-07-26)
The "Open Finance Admin Panel" button on Academics' super admin dashboard issues a handoff token and redirects to `{FINANCE_APP_URL}/superadmin/handoff/consume?token=...`, but Finance has no `/superadmin/handoff/consume` route at all - only `/handoff/consume` (headmaster/owner/parent roles, see `finance/app/Http/Controllers/HandoffController@consumeFromAcademics`), which has no branch for `role === 'super_admin'`. This was never actually built on the Finance side despite the Academics side existing and being wired up (its route just happened to live in a dead, unloaded routes file too - see the fix in this session's commits around `super_admin_routes.php` - so this bug was doubly hidden). Not fixed yet - needs a new Finance-side route + a `super_admin` branch in `consumeFromAcademics` (or a dedicated method) that logs into the `PlatformSuperAdmin` guard. Not blocking school onboarding, just a convenience cross-jump button.

## Resolved: Academics per-school DB provisioning was fundamentally broken (fixed 2026-07-26)
Confirmed root cause: this host's shared MySQL user does not have grants on arbitrary tenant databases, and `DatabaseHelper::createDatabaseUser()`'s raw SQL (`CREATE USER`/`GRANT`) silently failed every time (wrapped in a try/catch with a comment claiming "the main DB user has access to all databases in shared hosting" — false). Rebuilt to match Finance's already-working pattern:
- New `App\Services\DirectAdminService` (both apps) actually creates the database + a dedicated user via DirectAdmin's classic `/CMD_API_DATABASES` API (`action=create` works reliably; **its `action=delete` claims success but does nothing** — use the REST `/api/db-manage/databases/{db}` `DELETE` endpoint instead, which does work. Both services' `dropDatabase()` already use the working one.)
- `schools.db_username`/`db_password` store the real per-school credentials; `School::tenantConnectionConfig()`/`useAsTenant()` is the one canonical way to point the `tenant` connection at a school — replaced 15 separately-duplicated, inconsistent `configureTenantConnection()` implementations across controllers/middleware/commands with calls to it
- Verified end-to-end on sandbox: created a real test school via the super-admin panel, DirectAdmin provisioned a real dedicated DB+user, all 57 school-level migrations ran successfully against it, owner login worked

**Known UX gotcha:** creating a school runs 100+ migrations synchronously inside the HTTP request. This can exceed the web server's execution-time limit and show a **misleading 500 error page even though the school was actually created successfully** (verified: no Laravel exception logged, DB fully provisioned, all tables present) — always check the schools list before assuming a create failed and retrying, since retrying a "failed" create might attempt to reprovision a school that already exists. Not fixed (would need to move migration-running to a queued job) — just know to check before panicking or retrying.

**Also fixed the same underlying gap on Finance's live environment**: `finance/database/migrations/central/` (creates `super_admins`, `schools`, `school_accountants`, `activity_logs`, `analytics_summary`) had never been run against live at all — only sandbox has it, because sandbox uses a genuinely separate `central` database while live's `.env` consolidates `central` into the main database. This caused a 500 on every live Finance super-admin login (missing `school_id`/`user_type` columns on the pre-existing, differently-purposed `activity_logs` table) and would have blocked "Manage Schools"/"Activity Logs" too. Made all of `central/`'s migrations idempotent (`hasTable`/`hasColumn` guards) and ran them against live — safe to re-run anywhere now.

## Resolved (mostly): cross-system provisioning — onboarding a school into both Finance and Academics (2026-07-26/27)
Finance has a real, intentional "onboard both systems at once" feature (`has_finance`/`has_academics` checkboxes on `/superadmin/schools/create`), plus an "Enable Academics" form on an existing school's detail page for the "one system first, add the other later" case. Both were completely non-functional until this fix:

- **The bug:** `SchoolProvisioningService::provisionSchool()` (Finance) tried to run Academics' migrations *from within Finance's own PHP process* by pointing Laravel's migrator at Academics' folder path. This ran the wrong migration set (`database/migrations/`, Academics' own central/admin schema — users/roles/schools for Academics' *own* admin accounts) instead of `database/migrations/school/` (the actual per-school tenant schema — students/classes/exams/attendance/etc, same one `MigrateAllSchools` uses). It also would have run against *Finance's* `tenant` connection config, not Academics', since the migration files hardcode `Schema::connection('tenant')`. The "enable later" toggle didn't provision anything at all — just flipped a boolean. Live didn't even have the config this old code needed (`ACADEMICS_APP_PATH`/`ACADEMICS_MAIN_DB`).
- **The fix:** a proper internal HTTP API instead of cross-process migration-running. Academics provisions itself, in its own process, using its own already-correct `SchoolProvisioningService` (extracted from `SuperAdminController::createSchool()` so the web form and this API share one implementation) — see `academics/routes/internal_api.php`, guarded by a shared-secret header (`INTERNAL_API_SECRET` in both apps' `.env`, checked by `VerifyInternalApiSecret` middleware). Finance calls `POST /internal-api/schools` and, for the "enable later" case, the new `SchoolController::enableAcademics()` (not the old no-op toggle — that still exists for *disabling*, which doesn't need real provisioning).

**Residual reliability risk — genuinely unresolved, needs proper fixing:** this cross-app call runs 100+ migrations and can take minutes. This host's web server has been observed, across repeated testing, to:
1. Sometimes let the backend PHP process run to completion even after killing the client-facing connection (showing a misleading "Request Timeout" page while the work actually finishes correctly) — the same pattern as the standalone Academics create-school gotcha above.
2. But **at least once, actually killed the request mid-migration**, leaving a School row in Academics with an incomplete tenant database (missing tables) and no owner account — a genuinely broken partial state, not just a confusing success.

Because of (2), the response-reading code (`SchoolProvisioningService::provisionAcademicsSchool()`, Finance) can't just optimistically assume success when the HTTP response is unreadable — it calls Academics' `GET /internal-api/schools/exists` to check for real completion. That check deliberately verifies `school.user_id` is set (the last thing that happens, after migrations *and* owner-account creation both succeed and the transaction commits), not just that a School row exists, since the row is created *before* migrations run. If verification finds a row that exists but isn't complete, it throws a clear error telling the operator to manually clean up (drop the partial DB via DirectAdmin, delete the incomplete school row) before retrying with the same name.

**This mitigates the risk (no more silent false-positives) but doesn't eliminate it** — a request can still die mid-migration and require manual cleanup. The actual fix is architectural: move the whole provisioning flow to a queued background job so it isn't bound by any HTTP request's timeout at all. Not done — this is real, scoped follow-up work, not a quick patch (see spawned background task for it).

**Test data hygiene:** every test school created during this work was cleaned up afterward (dropped DB via the REST API — remember, not the classic API's delete — plus deleted the School/User/PlatformSchool/SchoolAccountant rows on both sides). If you see an unfamiliar school named anything test-sounding in either app's live-adjacent sandbox, check before assuming it's real data.

## Resolved: Finance accountant login never switched to the school's own tenant database (fixed 2026-07-27)
Confirmed root cause while testing fee entry/invoices end-to-end for the first time: `TenantDatabaseManager::switchToSchool()` — the only thing that points the `tenant` DB connection at a specific school — was called from school provisioning and the super-admin impersonation flow, but **never** from the normal accountant `/login` path. Every tenant model (`Student`, `SchoolClass`, `Voucher`, `Book`, ...) silently fell back to whatever `TENANT_DB_DATABASE`/`DB_DATABASE` resolves to by default — the *central* database on this environment — instead of the accountant's actual school. This had been invisible because only one Finance school existed on sandbox until this session (`HasSchoolContext`'s "if only one school exists, use it" fallback happened to paper over it); it would have caused real cross-school data collisions the moment a second school's accountant logged in.

Fixed with a new `App\Http\Middleware\EnsureTenantContext` (alias `tenant.context`), attached to the outer `['auth','verified']` route group in `finance/routes/web.php`. It switches to the authenticated `SchoolAccountant`'s own school (via the existing `school()` relationship), or the impersonated school for super-admins. **Any new accountant-facing route group added outside that `['auth','verified']` wrapper won't get this — check for that first if wrong-school data shows up again.**

**Not fixed as part of this pass:** Headmaster and parent portals resolve their school context through `HasSchoolContext`'s chain of fallbacks (app-container binding → env DB name → impersonation session → "only one school exists"), not through `EnsureTenantContext`. They likely have the same latent gap; out of scope for the fee-entry/invoices testing pass that found this, flagged here as real follow-up work, not fixed.

## Resolved: Finance fee-entry/student/class forms had several validation-vs-schema mismatches (fixed 2026-07-27)
Found while manually exercising Class Management, Student Management, Fee Entry, and Invoices end-to-end for the first time (previously untested per Claude memory's "Test coverage status"). All fixed same session, full detail in `debug-darasa`'s "Known error signatures":
- `school_classes.code` was required by validation but never collected by the Add/Edit Class form (422 on every class creation) — now optional, auto-derived from the name.
- `school_classes.level` was an `unsignedInteger` column but the Level dropdown always sent a string (`SQLSTATE... Incorrect integer value`) — widened to varchar via a new migration.
- Student create/update validated `gender` as lowercase but the dropdown sends `"Male"`/`"Female"` (422 on every student creation) — normalized server-side.
- `students.admission_date` was read/written everywhere in the student create/edit/show flow but was never actually migrated (`Unknown column 'admission_date'`) — added via a new migration.
- `VoucherController::store()` only updated the `particular_student` balance pivot if a pivot row already existed, silently skipping a student's *first-ever* voucher for a given particular — fixed to `attach()` the pivot first if missing.
- A duplicate pair of `downloadStudentTemplate()`/`uploadStudentCsv()` methods in `StudentController` (added by the CSV-import feature commit without removing the originals) caused a PHP fatal "Cannot redeclare", 500ing every route that touched `StudentController` (the whole Students page). Removed the duplicates, kept the pair matching the real schema.
- Invoice PDF's "Academic Year" header used an emoji DomPDF can't render, showing a broken `?` glyph on every generated invoice — removed.

**Gap this exposed:** Finance has no bulk "rerun a new tenant migration against every already-provisioned school" command (Academics has `migrate:schools`; Finance doesn't). The `level`/`admission_date` migrations above only reach schools created *after* the fix — any pre-existing school's tenant DB needs the same `ALTER TABLE` run manually via its dedicated `db_username`/`db_password` credentials (see `SchoolProvisioningService`'s per-school migration call at DB-creation time for the pattern being reused). Worth building a proper Finance equivalent of `migrate:schools` if more schema drift like this turns up — not done yet, real follow-up work.

## Resolved: Academics tenant DB connection was never actually switched for any real school-level login (fixed 2026-07-27)
The headline bug found while testing Class Management/Student Management/Attendance/Exams end-to-end for the first time. `InitializeTenantDatabase` middleware — the only thing that reads `session('school_db')` and points the `tenant` connection at the right school on every request — was registered via the bare `$middleware->append()` in `academics/bootstrap/app.php`. In Laravel 11 that runs in the global stack, **before** the `web` group's `StartSession` middleware, so `session('school_db')` always read null inside it and its tenant-switch guard silently no-opped. Every school-level page (school-admin/owner/headmaster/teacher/student/academic dashboards — anything reading `DB::connection('tenant')` outside a request that called `useAsTenant()` directly, like login itself) hit the unconfigured placeholder `tenant` connection (`olamtecc_mivumoni`) instead of the real school. This had been invisible because nobody had exercised a real school-level login end-to-end before this session (Academics testing to date had been super-admin/system-admin only, which don't hit this path). Fixed by moving it into `$middleware->web(append: [...])` so it runs after `StartSession` like the rest of this app's session-dependent middleware.

## Resolved: Academics staff/student/role provisioning had several latent gaps (fixed 2026-07-27)
Found in the same testing pass, all fixed same session, full detail in `debug-darasa`'s "Known error signatures":
- `RoleSeeder` (Admin/Owner/Headmaster/ClassTeacher/Teacher/Student/Academic) was never called from `SchoolProvisioningService::provision()` — every real school had an empty `school_roles` table, breaking any Role dropdown anywhere.
- The school detail page's "Manage Staff" button 500'd (`RouteNotFoundException`) — same "route only exists in the dead `routes/super_admin.php` file" pattern as the already-documented Finance↔Academics cross-jump bug.
- `SuperAdminController::tenantForSchool()` hand-rolled the tenant connection using the shared central DB user instead of each school's own dedicated credentials (`Access denied` on every staff-management action) — replaced with `$school->useAsTenant()`, the already-established canonical method.
- Assigning a system admin to a school (`assignSystemAdmin()`) never set `registration_no` — registration number is the only login credential this app accepts, so the account could never log in. Fixed by reusing the sequence-generation pattern already correct in the sibling `createSystemAdmin()` method.
- Both student-creation paths (Add Student form, bulk CSV import) hardcoded `role_id = 7` believing it meant "Student" — it's actually "Academic" per `RoleSeeder`'s real order (Student is 6). Every student created was misfiled as staff. `OwnerController::manageStaff()` had the mirror-image bug. Fixed by looking role ids up by name instead of hardcoding numbers.
- Staff created via `SchoolAdminController::createStaff()` never got a row in the separate `teachers` table, so Teacher/ClassTeacher staff could never be assigned to a class (`Assign Teachers` dropdown was silently empty).

**Not fully fixed — real follow-up work:** `OwnerController::addStaff()` and `updateStaff()` have the same class of role-id/name mismatch bug throughout (wrong numbers in per-package role-limit tables, error-message role-name labels, and the class-teacher-assignment conditional checks role 5 "Teacher" where the comments say "Class Teacher", which is role 4). Not touched — fixing it correctly means verifying the actual intended per-package (core/standard/premium) role-limit business rules, which nothing in this testing pass could confirm. Audit and fix `OwnerController`'s staff methods against the real `RoleSeeder` order before relying on them.

## Feature: Academics attendance template now supports upload, not just download (added 2026-07-27)
The attendance Excel template previously put one column per day across the record's *entire* date range on a single sheet — a Term 1 record (Jul–Dec) needed 150+ columns on one sheet, and there was no way to get a filled-in template back into the system at all (only download existed).
- `AttendanceTemplateExport`/`AttendanceMonthSheetExport` now generate one worksheet per calendar month, each with a teacher-editable "Attendance valid through" cutoff cell at the top (row 1) and that month's own header row of dates (row 3 — see debug-darasa for why not row 4).
- New `POST /owner/attendance/upload-template/{id}` (`AttendanceController::uploadAttendanceTemplate`) parses each sheet, honors its own cutoff (clamped to today and to the record's date range), matches students by registration number, and **upserts** `attendance_entries` per (attendance_id, student_id, date) — X = absent, blank/anything else = present, same "default present, teacher unticks" convention as the direct Take Attendance UI.
- Fixed alongside it, a real data-loss bug: `submitAttendance()` (the direct-edit path) used to delete *every* date's entries for an attendance record before re-inserting just today's, so taking attendance on a new day silently erased all previous days. Now scoped to just today's date — this is also what makes it safe to mix direct edits with template uploads, since neither can clobber the other's dates anymore.
- Verified end-to-end: downloaded a real template (6 month-sheets for the Jul–Dec test record), edited a cell to mark a student absent, uploaded it, confirmed the DB status flipped from present→absent: inserted a fake prior-day entry directly, submitted today's attendance via the UI, confirmed the prior day survived.

## Resolved: Finance school name showed as literal "School Name" everywhere (fixed 2026-07-27)
`App\Models\SchoolSetting` hardcoded `$connection = 'mysql'` with a comment admitting it was "for single-school setup" — same class of bug as the accountant tenant-switching fix above, just missed the first time since it's a model, not a controller/middleware. All 14 call sites of `SchoolSetting::getSettings()` (dashboard header, invoice PDFs, settings page, ...) queried the default `mysql` connection instead of the tenant-switched school database, found no settings row there, and the method's fallback silently auto-created one with `school_name` literally `'School Name'`. The real settings row (correct name, phone, etc.) was sitting untouched in the school's own tenant DB the entire time. Fixed by switching to `$connection = 'tenant'`. Verified: header and a re-downloaded invoice PDF both now show "OLAM SECONDARY SCHOOL" instead of the placeholder.

## Resolved: Finance ledgers, overdue tracking, and suspense accounts — full re-test pass (2026-07-27)
Re-tested these three modules on sandbox as planned follow-up to the fee-entry pivot fix (see the accountant tenant-switching entry above). Found and fixed real bugs in every one of them — full detail in `debug-darasa`'s "Known error signatures":

- **Ledgers (the headline bug):** `studentLedger()`/`classLedger()` and their PDF/CSV exports computed balance as raw `debit - credit` on `Voucher`, but canonical storage keeps *both* Sales and Receipt amounts in `debit` — so recording a payment increased the student's Outstanding balance instead of decreasing it. `particularLedger()` already had the correct DR/CR mapping in a private method; extracted it as `LedgerController::ledgerDisplayAmounts()` and reused it everywhere. Also fixed: the "View Class Ledger" button called a `viewClassLedger()` JS function that didn't exist (a prior edit had merged its body into `viewStudentLedger()`, left dangling after that function's own catch block); and Class Ledger printed "TSh NaN" for Opening Balance when no date filter was applied (`null` vs `0`).
- **Overdue tracking:** the core logic (`DashboardController::getOverdueAmounts()`, filtering `particular_student` by `deadline < now()`) is correct and was verified working once a real deadline existed — the reason it showed "no overdue students" initially was that the sandbox's only test fee assignment was created via ad-hoc Fee Entry (which never sets a deadline), not the "Assign Particular to Student" flow. That flow itself was blocked by there being zero Academic Years on the sandbox school — Academic Years now have a working Settings-page UI (`+ Add Academic Year`), used to unblock this test.
- **Suspense accounts:** create → resolve → student ledger / book ledger all verified consistent end-to-end (a resolved suspense entry becomes a real Receipt against the chosen particular via a Reversal + Resolution voucher pair, and the cash book nets to the right total, not double-counted). Two real bugs fixed along the way: the Settings page's Bank Accounts section 500'd on every load (`BankAccountController` referenced two columns — `display_order`, `school_setting_id` — that have never existed on `bank_accounts`, and never collected the NOT-NULL `account_name` field either, so even a fixed list view couldn't actually save a new bank account); and the Resolve modal's "Amount Required" field always showed TSh 0.00 (read `debit` instead of `sales` from the particular-details endpoint).
- **Also fixed in passing:** `particulars.blade.php`'s single-student assignment flow saved correctly to the database but crashed the UI refresh afterward with a null-dereference on dead, unused code.

## Resolved: Academics report cards and timetables — full re-test pass (2026-07-27/28)
Re-tested these two modules on sandbox as planned follow-up items. Timetables (document upload/download/filter/delete) worked cleanly with zero bugs found. Report Cards turned up five real, previously-invisible bugs across the whole create→generate→publish→PDF flow — full detail in `debug-darasa`'s "Known error signatures":

- **The headline bug:** the entire `<script>` block on 6 different modernLayout-based pages (Report Card create/create_enhanced, the Academic dashboard, Check Student Results, super-admin SMS balances, the Teacher dashboard) was silently never rendered at all — they used `@push('scripts')`/`@endpush`, but no layout in the chain actually outputs `@stack('scripts')` (the real mechanism here is `@section('page-script')`, already used correctly elsewhere). This made the Report Card creation page's "Add Exam" button, live percentage total, and submit-button gating all completely dead — confirmed via `document.getElementById('addExamBtn').click()` firing nothing, not even a JS error.
- **A second, independent bug in the same form:** even after fixing the script-loading issue, "Create Configuration" still silently refused to submit at exactly 100% — a hidden template row (used only as an `innerHTML` clone source) sat inside the `<form>` with its own `required` fields, and this browser's native HTML5 validation still counts `display:none` descendants with `required`+`name` as validation candidates. Fixed by moving the template outside the `<form>` entirely.
- **`ClassModel` (the `classes` table model)** never declared the tenant connection that every other tenant model has, so any call site that forgot the per-query `::on('tenant')` override 500'd against the central platform DB instead of the school's own — this is what made the Academic dashboard itself 500 before any of the report-card testing could even start.
- **Three spots called `$student->classModel`** (should be `$student->class` — `classModel()` only exists on the unrelated `SubjectPerformance` model): the Academic dashboard's student-detail panel and student-filter dropdown, and the Owner dashboard's "Student Distribution by Class" widget (silently swallowed by a `try/catch`, so it just looked like "no data").
- **Two spots read `$student->registration_number`** (real column is `registration_no`) — Report Cards' list view and per-student comments page always showed "N/A" for a real registration number.

All fixed and verified end-to-end: created an Academic-role test account (none existed before this session — `Peter Academic` / `S0017001`), created a report card configuration, generated it, verified a mark, published it, downloaded the PDF (200, correct data), and saved a comment — all working correctly on sandbox as of 2026-07-28.

**Follow-up audited and closed (2026-07-28):** the five other Academics models flagged with the same missing-`$connection='tenant'` gap (`Location`, `Role`, `SchoolAdmin`, `LogModal`, `SmsBalance`, `User`) are **not bugs** — every usage of all five is exclusively in super-admin/system-admin/platform-management controllers (`SuperAdminController`, `SystemAdminController`, `AdminController`), never in any school-level/tenant request path. They're legitimately central/platform-wide records (one shared `Location`/`Role`/`SmsBalance` table across *all* schools, not per-school tenant data) — `School` itself is also correctly on the default connection for the same reason. Adding `$connection = 'tenant'` to any of them would have actively broken super-admin functionality by pointing platform-wide data at whatever school happens to be in session at the time.

## Feature: Finance parent portal now logs in with an auto-generated portal_email, not student_reg_no (2026-07-28)
Previously a parent logged in with the student's `student_reg_no` — a free-text field an accountant can set to anything, with no relation to (and no reason to resemble) Academics' role-based `registration_no` format. Replaced with a new `students.portal_email` column: an auto-generated, per-school-unique identifier shaped like `firstname[N]@{school-set domain}.com` (first "Jackson" gets `jackson@...`, a second gets `jackson1@...`, and so on — collision-checked at generation time, never manually typed).

- New Settings field, **Parent Portal Email Domain** (e.g. `olam` → `jackson@olam.com`), admin-set once per school.
- Generated automatically on student creation (`StudentController::store()` and the CSV bulk-import path); a **Generate** button on the Portal Passwords page backfills it for students added before the domain was configured (never overwrites an existing one).
- The `portal_password` mechanism is unchanged — same hash, same admin-set flow, just checked against `portal_email` instead of `student_reg_no` at login (`ParentAuthController`).
- `student_reg_no` itself is completely untouched — the accountant still uses it internally exactly as before; only the parent-login identifier changed.
- Removed `HandoffController::issueParentFromFinance()` and the parent branch of `consumeFromAcademics()` — dead code (no UI button ever called the issue side, and Academics has no parent account concept at all, so it could never legitimately issue a `'parent'`-role token to be consumed). Headmaster/owner handoff is untouched.

Building and testing this feature end-to-end (there was no prior parent-portal test coverage at all) surfaced four unrelated, previously-invisible bugs along the way — all now documented in `debug-darasa`'s "Known error signatures": the parent portal never switched the tenant DB connection at all (same bug class as the already-fixed accountant/Academics gaps — new `EnsureParentPortalTenantContext` middleware fixes it), `Student::$fillable` was missing the three `portal_password*` columns entirely (every "Set Password" click silently no-op'd while reporting fake success), the parent dashboard/Fees page/statement PDF had the same ledger balance-sign bug as the Finance Ledgers fix earlier this session (now centralized as `Voucher::displayAmounts()`), and the Fees page showed a hardcoded, unrelated "Academic Year 2024" label.

**Not fixed, confirmed as a real bug (not just "likely" anymore):** Headmaster login has the exact same tenant-connection gap the parent portal just had — `Headmaster` is also a `$connection = 'tenant'` model, and `HeadmasterAuthController` never switches the tenant connection before querying it. Out of scope for this pass (a separate, already-existing feature, not something this session's changes broke), but now a confirmed, not speculative, gap — see Claude memory's Pending section.

## Resolved: Finance headmaster portal never worked, plus a systemic route-model-binding ordering bug affecting every tenant-scoped model (2026-07-28)

Testing the headmaster portal end-to-end for the first time (login → dashboard → ledgers → overdue → invoices) turned up the confirmed gap flagged above, plus three more bugs stacked underneath it:

1. **Tenant connection never switched** — same fix pattern as the parent portal: new `EnsureHeadmasterTenantContext` middleware (alias `headmaster.tenant.context`), applied to both the guest and authenticated route groups in `routes/headmaster.php`.
2. **Headmaster management UI had no way to ever set a password** — the "Add headmaster" form had no password field, and the table had no reset/set-password action, even though the backend route (`accountant.headmasters.reset-password`) already existed and was already correctly wired. A headmaster could only ever log in via the platform super-admin's master-password override. Fixed by adding password/confirm fields to the create form and a Set/Reset Password modal per row.
3. **Systemic: route-model binding resolved before the tenant DB was switched, on *any* route with a bound model parameter.** `SubstituteBindings` sits at the end of Laravel's default `web` middleware group; none of this app's tenant-switching middleware had an explicit priority, so `{headmaster}`/`{student}`/`{class}`/etc. route params were resolved against whatever the `tenant` connection defaults to (the central DB), not the actual school. Reads via implicit binding could silently return a different school's record; writes happened to look correct because Eloquent re-resolves the connection at save time (after the tenant middleware runs), and both a stray leftover `headmasters` row in the central DB (from an old ad-hoc setup script) and the real school's headmaster happened to share id `1`. This would corrupt data across two real schools whose IDs diverge — a real correctness risk, not just cosmetic. Fixed via `$middleware->prependToPriorityList(before: SubstituteBindings::class, ...)` for all three tenant middlewares in `finance/bootstrap/app.php`.
4. **Headmaster layout never rendered flash messages at all** — `layouts/headmaster.blade.php` had no `@if(session('success'))`/`@if(session('error'))` blocks (unlike the accountant layout it was copied from), so any failed action (e.g. the "Open Academics" SSO handoff button, which correctly denies headmasters without cross-jump access) looked like it silently did nothing. Fixed by adding the same flash blocks.

Two separate ad-hoc root-level PHP scripts (`full_setup.php`, `setup_system.php` — plus `fix_accounts.php`/`reset_passwords.php` with slightly different data) exist in the Finance repo root and insert directly into whatever the *default* DB connection is at the time they're run. These are why the central DB has leftover tenant-shaped tables (`headmasters`, `students`, ...) with test data — not part of the app's normal runtime, but a real source of confusing "which DB is this row actually in" bugs if ever run again. Not removed this session (out of scope), but worth knowing they exist before debugging anything that looks like data appearing in the wrong place.

## Resolved: Academics had the exact same route-model-binding-before-tenant-switch bug (fixed 2026-07-29)

Checked on request right after the Finance fix above. `academics/bootstrap/app.php` appended `InitializeTenantDatabase` to the `web` group with no explicit priority — same structural gap. Confirmed real, live impact (not just theoretical): `AssignmentController::update(Request $request, Assignment $assignment)` binds `Assignment` (a `$connection = 'tenant'` model) implicitly, and that route is reachable from the headmaster, owner, and teacher portals alike. Two things in the existing code suggest a previous developer had already run into symptoms of this without finding the root cause: `update()` has a manual `$this->switchTenantDB()` call inside its body (redundant with the global middleware, and too late to fix the binding itself), and the route group re-applies `InitializeTenantDatabase` as route-level middleware on top of the global one (also doesn't help — `SubstituteBindings` still resolves inside the `web` group before route-level middleware runs). Verified fixed on sandbox via a direct DB insert + an authenticated `PUT /assignments/{id}` request: the update correctly landed on the intended row in the real tenant DB. Fixed identically to Finance: `$middleware->prependToPriorityList(before: SubstituteBindings::class, prepend: InitializeTenantDatabase::class)`.

**Found while auditing this, fixed on request 2026-07-29:** `AssignmentController::create()` returned a view (`content.dashboard.owner.assignments.create`) that didn't exist on disk for any role — the entire "create assignment" UI 500'd regardless of portal. The `edit()` method referenced by the `GET /assignments/{assignment}/edit` route didn't exist in the controller at all. Fixed by adding `create.blade.php`/`edit.blade.php` and the `edit()` method, and replacing `index.blade.php`'s dead (commented-out) Edit-modal trigger with a real link to the new edit page.

**Also fixed on request 2026-07-29:** Notes (Assignments' sibling feature, same controller) had a completely dead "Edit Note" modal + JS wired to a `PUT /notes/{id}` route that never existed — no backend for editing a note existed at all (unlike Assignments, where the backend existed and only the view was missing). Add/list/download/delete were tested and confirmed already correct. Fixed by adding `notesEdit()`/`notesUpdate()` + routes (registered in `teacher_routes.php` only — Notes was never wired into headmaster/owner portals, unlike Assignments) + `edit.blade.php`, wired a real Edit link into the list in place of the dead modal.

## Resolved: Finance — Books, Expenses, Payroll, Student Promotion, Bank Integration deep-testing pass (2026-07-29)

User asked to test every remaining untested Finance module before the app goes into real use, deploying to live only once the whole pass is done. Found and fixed real bugs in every module touched — consistent with the pattern established earlier in this project that nothing untested should be assumed to work.

**Books:** `SchoolProvisioningService::seedDefaultData()` inserted every new school's default Cash Book with `type='cash'` but never set `is_cash_book=true` — so the one book that's supposed to be protected (no Edit/Delete/Fees-Cuts, shown as "💵 Cash Book") instead displayed and behaved exactly like a regular bank account for every school ever provisioned. Fixed the provisioning code and the existing sandbox school's row. Separately: withdrawing with a transaction fee category correctly creates a linked fee `Voucher` on the backend (`BookTransactionController` already eager-loads `feeVoucher`), but the Transaction History modal never rendered it — an accountant would see "Withdrawal: 100,000" with no indication a 1,500 fee was also deducted, even though the book's real balance reflects both. Fixed by showing the fee inline under the amount (initially wired the JSON key as `feeVoucher`/camelCase per the Eloquent relation method name — Laravel actually serializes it as `fee_voucher`/snake_case by default, corrected after the first version silently showed nothing).

**Expenses:** three separate schema-vs-code mismatches, each blocking expense creation/processing outright: `expense_number` is unique+NOT NULL with no default, but nothing ever populated it — every create failed with a duplicate-null-key error (fixed with the same auto-increment pattern `Voucher::boot()` already uses for `voucher_number`); `category`/`description` were NOT NULL with no default while the controller/validation treats both as optional — creating an expense without a description 500'd; `processed_at` didn't exist as a column at all, so "Process" always failed. All three confirmed via real create→process flow on sandbox after fixing.

**Payroll:** `payroll_entries` had a legacy scalar `deductions` *column* left over from before the 2026-06-18 schema fix, which silently shadowed the real `deductions()` HasMany relationship everywhere a payroll entry was accessed (Eloquent always prefers a real column over a same-named relation) — confirmed via a real 500 ("Call to a member function sum() on string"). Dropped the dead column. Separately, `storePayroll()` built the salary `Voucher` using `narration`/`reference_number` keys, neither of which is in `Voucher::$fillable` — both were silently dropped by mass assignment, so every payroll voucher showed up in the ledger with a blank description. Fixed to use the real fillable columns (`payment_by_receipt_to`/`notes`). Also added `updatePayroll()`, which `PUT /api/payroll/{id}` routed to but which never existed in the controller (no UI currently calls it, but the route was live and would 500 if it ever did). Verified end-to-end: added a staff member, processed a real payroll entry, confirmed it in Payroll Entries.

**Student Promotion:** `promotionPage()` fetched classes with no `withCount()`, but the view renders `$class->active_students_count` for every dropdown option — every class showed "Form One ( students)" with a silently-blank count (Blade echoes null as empty, no error). Fixed by adding the count.

**Bank Integration:** both `bank_transactions` and `bank_api_settings` were written against a schema that was redesigned at some point but never actually migrated — `BankAPIController`/`BankTransaction` reference columns (`control_number`, `bank_name`, `account_number`, `processing_status`, `sms_sent`, `payer_phone`, `is_reconciled`, direct student/book/voucher/suspense_account links on the transactions side; `use_simulation`, `webhook_secret` on the settings side) that simply didn't exist — every webhook, "simulate payment," and settings-save call 500'd with an "Unknown column" error. Added the missing columns to both tables (left the old, now-unused columns in place rather than risk data loss renaming them — no real bank_transactions rows exist anywhere yet).

**Deploy note:** the five migrations for this pass were committed but not run against the sandbox tenant DB in the same turn they were written (a transient loss of connectivity to the hosting server interrupted the session) — caught and run in a follow-up turn before trusting any of the above as actually verified. Bare `php artisan migrate` on this app affects both the tenant DB and (harmlessly) the shared central DB, since none of these migrations call `Schema::connection('tenant')` explicitly — worth remembering if a future migration needs to be tenant-only.

**Also fixed in this pass, found while exercising Students and the FAQ chatbot:**

- **Editing any student silently failed validation unless the accountant manually re-picked Gender.** The Edit Student modal's gender `<select>` used capitalized option values (`Male`/`Female`) matching the Add-student form, but `student.gender` from the API is always lowercase (`store()`/`update()` both normalize it before saving) — `select.value = 'female'` against an option only valued `'Female'` matches nothing and silently resets to blank. Every save then 422'd with "the gender field is required" unless the field happened to get manually reselected. Lowercased both the edit-modal and list-filter option sets.
- **The student list's search box and gender filter did nothing at all.** `apiIndex()` only ever implemented `class_id`/`status`, silently ignoring the `search`/`gender`/`sort` params the front-end already sends (the sort dropdown's 5 options were pure decoration — results were always name-ascending regardless of selection). Implemented all three.
- **"Edit Student" did nothing (a suppressed alert, no visible modal) for every student, every time.** `editStudent()` awaited a call to `/students/{id}` first "to fetch fresh data," then immediately fetched the real data a second way and used that instead — its own comment admits the first call's result was never used. That first call hits `StudentController::show()`, which renders `resources/views/students/show.blade.php`, a leftover from an earlier iteration of this app (references `$student->invoices`/`payments` and KES currency — concepts that don't exist in the current voucher/particular/TSh architecture) and always 500s. Since it was awaited before the working second call, the whole function aborted on every click. Removed the dead first call.
- **The FAQ chatbot's tenant connection was never switched — the 6th occurrence of this exact bug class this session.** `/api/assistant/*` only ran through `EnsurePortalSession` (an auth/session check, nothing else) — no tenant-context middleware at all. Confirmed live: "Collections this month" and "Fee collections today" both answered "TSh 0" despite real receipts existing for both periods, because every query ran against whatever the `tenant` connection defaults to (the central DB) instead of the school's real database. This has likely been silently wrong for every quick-question and free-text query since the feature shipped, for accountant, headmaster, and parent audiences alike. Fixed by stacking the three existing tenant-context middlewares ahead of `portal.session` — verified after the fix: "Collections this month" correctly returned TSh 125,000, "today" TSh 50,000, and a free-text "how many students have outstanding balances?" correctly returned TSh 75,000 net of scholarships — all matching hand-calculated totals from the real test vouchers.

## Resolved: SMS gateway was never actually configured with a real API token; Student Promotion confirmed data-safe (2026-07-31)

User asked two follow-up questions after the deep-testing pass above: whether Student Promotion could lose overdue/advance data across a class change, and to actually try sending a real SMS (not just exercise the credit-guard, which was all that had been tested before).

**Student Promotion:** confirmed data-safe by both code inspection and a real test. `promoteStudents()` (`StudentController.php`) only ever does `Student::whereIn('id', ...)->where('class_id', $source)->update(['class_id' => ..., 'class' => ...])` — no other table or column is touched. Overdue tracking (per-particular) and `advance_balance` (a column on `students`) are both keyed by `student_id`, never `class_id`, so a class change structurally cannot affect them. Verified with a real test: gave Asha Mwangi a TSh 40,000 overdue Uniform Fee balance and a TSh 40,000 advance balance while in Form One, promoted her to Form Two via the real API, and confirmed via Overdue Payments, Advance Payments, and the full Student Ledger that all of it survived intact — only the class label changed.

**SMS gateway had no API token configured anywhere.** A real send attempt (to a real number, `0627404843`, set as Asha's Parent Phone 2) failed with a generic "API error" — not an exception, just `$response->successful() === false` from the NextSMS call in `SmsController` (`Http::post($this->apiUrl.'/api/sms/v2/text/single', ...)` with `Authorization: Bearer ' . $this->apiToken`). Root cause: `SMS_API_TOKEN` was never set in `.env` on **either sandbox or live** — `env('SMS_API_TOKEN')` returned `null`, so every real send went out with a literal `Authorization: Bearer` header and empty token, and NextSMS correctly rejected it. Credits were never decremented on failure, confirming the credit-guard logic itself was already correct — this was purely a missing-credential gap, not a code bug. User supplied a real NextSMS API token mid-session; added `SMS_API_TOKEN=<token>` to both `.env` files (see `project_darasa_finance.md` memory for the actual value — not duplicated here) and ran `php artisan config:clear` on both. Re-tested the real send on sandbox afterward: `{"success":true,"sent":1,"failed":0,"sms_credits":{"used":1,"remaining":9}}` — confirmed working end-to-end for the first time.

**Dashboard UI:** moved the SMS module card to the last position in the accountant dashboard's "All modules" grid (`resources/views/admin/accountant/dashboard.blade.php`) per explicit request — a pure reorder, no logic change. Verified visually on sandbox, deployed to live.

## Feature: SMS delivery reports + resend failed messages (2026-07-31)

User pointed out the shared NextSMS sender name ("DARASA 360") is intentional platform-wide branding for all schools (not a bug to fix), and asked for a real delivery-report feature: know when an SMS actually delivers or fails, and be able to resend a failed one (optionally to a different phone number).

**Delivery webhook.** NextSMS POSTs `{"token": "...", "messageId": "...", "status": "DELIVERED", "id": "..."}` to a callback URL configured on their own dashboard. Added `POST /api/sms/delivery-callback` (public, no auth) in `SmsController::deliveryCallback()`, verified via a shared secret (`SMS_DELIVERY_CALLBACK_TOKEN` in `.env`, not a Laravel auth guard) since an external gateway can never carry a session/CSRF token. **Live callback URL to give NextSMS: `https://finance.darasa360.co.tz/api/sms/delivery-callback`** (sandbox: same path on the sandbox domain).

**Routing the callback to the right tenant DB was the interesting part.** One NextSMS account serves every school (shared sender name, shared token), so the callback arrives with zero session/school context — but `SmsLog` lives in each school's own tenant database. Solved with a new central-only lookup table, `sms_delivery_index` (`message_id` → `school_id`), written at send time (all three send call-sites: `sendSms()`, `sendBulkSms()`, `sendOverdueReminders()`) right after NextSMS returns a real `messageId`. The callback looks up the school by `message_id`, calls `app(TenantDatabaseManager::class)->switchToSchool($school)` to point the `tenant` connection at the right database, then updates the matching `SmsLog` row's `status`/`delivered_at`.

**Found a real schema-vs-code mismatch while building this:** `SmsLog::$fillable` already listed `reference` and `delivered_at`, and the SMS Logs blade view already rendered a "Delivered" status + `delivered_at` timestamp — but neither column actually existed in `sms_logs` on any real database (confirmed via direct query against the real per-school tenant DB, see the CLI-access gotcha below). This UI had clearly been built ahead of a delivery-report feature that was never finished. Added both columns via a new migration.

**CLI access to a real tenant database needs the SCHOOL's own DB credentials, not the shared `.env` ones.** Discovered while trying to inspect `sms_logs`' real schema: `mysql -u olamtecc_darasa_user ...` (the shared `.env` `DB_USERNAME`/`DB_PASSWORD`) gets `Access denied` for any per-school database — that shared user only has grants on the central/platform databases. Each school has its own dedicated MySQL user + password stored on its `schools` row (`db_username`/`db_password`), and `TenantDatabaseManager::switchToSchool()` swaps `config('database.connections.tenant.*')` to those before reconnecting — that's the only thing that actually has permission on a real tenant DB. To run a migration against an already-provisioned school's real tenant DB (not just a newly-provisioned one, which `SchoolProvisioningService` already handles correctly), replicate that call manually: write a small PHP script (tinker chokes on inline `--execute` with namespaced classes over SSH — use `php artisan tinker /path/to/script.php` with a real file instead) that does `$school = School::find($id); app(TenantDatabaseManager::class)->switchToSchool($school); Artisan::call('migrate', ['--database'=>'tenant','--path'=>'database/migrations','--force'=>true]);`. Running this against the sandbox school for the first time also revealed five *earlier* migrations (2026-07-26 through 2026-07-29, from the Expenses/Payroll/Bank-Integration fixes) had never actually been recorded in that tenant's own `migrations` table — they'd evidently been applied by hand via direct SQL rather than through Laravel's migrator. Not a new problem, just newly visible; the migrate command applied all of them cleanly and idempotently.

**Resend.** New `POST /sms/logs/{logId}/resend` (accountant-authenticated), gated to `status === 'failed'` only, with an optional `phone` override. Creates a fresh `SmsLog` row (`reference` set to `resend_of_{id}`) rather than mutating the failed original, so history stays intact. Wired into the SMS Logs page with a "Resend" button per failed row, opening a small modal pre-filled with the original number so the accountant can edit it before retrying.

**Also fixed in passing:** `logsAccountant()` completely ignored the `status`/`from_date`/`to_date` filters the SMS Logs page's own filter form already sent — the UI looked functional but silently showed everything regardless of the selected filter. Implemented all three.

**Found but deliberately not fixed (flagged as a separate task instead):** the pre-existing `/api/bank-webhook` route has the identical CSRF gap this feature's webhook needed guarding against, but was never exempted - a real external bank POST would likely 419. Never surfaced before because only the authenticated "Simulate Payment" button (which carries a valid session token) has ever exercised that path. Spawned as a follow-up, not fixed here to keep this change scoped.

## Resolved: DirectAdmin school-creation intermittently (then consistently) failed with "Not logged in" — first live school onboarded (2026-07-31)

User tried to onboard the first real live Finance school (LITTLE DOVES DAYCARE AND NURSERY SCHOOL) via the normal superadmin "Create New School" UI. It failed repeatedly with `Failed to create Finance database for school: ...`, with the underlying DirectAdmin response always `Unauthorized&error=1&text=Not+logged+in` — despite the login key, IP allowlist, and DB credentials all being unchanged and previously working.

**Extensive misleading red herrings before the real cause:** the host's load average spiked into the 50s-60s mid-investigation with SSH connections being killed outright (a real, separate hosting-infrastructure problem, unrelated to this bug, that made diagnosis much slower and noisier) — this looked at first like the obvious explanation. It wasn't. Manual `curl` reproduction of the exact same DirectAdmin API call, with identical credentials and parameters, *always* succeeded regardless of server load, while the app's own Guzzle-based call *always* failed — a clean, reproducible client-specific difference, not a load issue.

**Real root cause, found via Guzzle's own debug output (`Http::withOptions(['debug' => true])`, which pipes libcurl's verbose trace to stdout under `php artisan tinker`):** `vda6000.is.cc` resolves to a link-local IPv6 address (`fe80::...`) in addition to its real IPv4 (`174.138.190.170`), and Guzzle/curl on this server was connecting over that IPv6 address by default. DirectAdmin's login-key IP allowlist is IPv4-only (`174.138.190.170`) — a request arriving over the mismatched IPv6 address gets rejected, but DirectAdmin reports this as a generic `Not logged in` rather than an IP-restriction error, which is what made it look like an auth problem. Confirmed conclusively by adding `'force_ip_resolve' => 'v4'` to a one-off test call — it succeeded immediately. **Fixed** in `DirectAdminService::post()` and `dropDatabase()` by adding `'force_ip_resolve' => 'v4'` (plus `'allow_redirects' => false` defensively — not the actual cause here, but Guzzle drops the `Authorization` header on redirect by default, which is worth guarding against regardless). Deployed to both sandbox and live.

**Also added:** `db_name_slug` — an optional override in `provisionSchool()`'s `$data` array that lets a custom slug generate the tenant database name instead of one auto-derived from the full school name. Added because one specific auto-generated name (`school_001_little_doves_daycare_and_nurse`) was suspected of being individually blocked before the real IPv6 cause was found; kept as a permanent feature since it's a genuinely useful escape hatch, not just a workaround for this incident.

**Two more gotchas hit while manually re-running `provisionSchool()` via `tinker` to complete the onboarding (both CLI-only artifacts, not real bugs a normal superadmin UI session would ever hit):**
- `ActivityLogger::logSuperAdminAction()` requires `auth('superadmin')->user()` to be non-null — a real browser session always has this, but a CLI script doesn't unless it explicitly does `Auth::guard('superadmin')->login($superAdmin)` first. The superadmin guard's actual model is `App\Models\Platform\PlatformSuperAdmin` (config/auth.php's `superadmins` provider) — easy to get wrong (there's no `App\Models\Central\SuperAdmin`), and doing so makes the login silently no-op instead of erroring, which wastes a full retry.
- **`dropDatabase()`'s rollback cleanup does not reliably remove the MySQL *user* created alongside a database, even though it does remove the database itself.** Every failed `provisionSchool()` attempt creates both an `olamtecc_school_XXX_<slug>` database and a deterministically-named MySQL user (`substr(preg_replace('/[^a-z0-9]/','',$name),0,6) . substr(md5($name),0,5)`, prefixed) via `DirectAdminService::createDatabase()` — on rollback, the DB gets dropped but the *user* can be left behind, so a retry with the identical name (same input → same deterministic username) fails with `That user already exists`, distinct from `That database already exists`. Both need deleting via DirectAdmin's REST endpoint (`DELETE /api/db-manage/databases/{name}` and `DELETE /api/db-manage/users/{name}`) before a clean retry — checking only the database list undercounts what's actually left over. Worth fixing properly (make `dropDatabase()` also drop the associated user) if this keeps recurring; not fixed this session to avoid scope creep on an already-long incident.

**Outcome:** LITTLE DOVES DAYCARE AND NURSERY SCHOOL is now real school id 17 on live, database `olamtecc_school_001_littledoves`, accountant Renatus Revelelian Kamala (`littledovesdaycare2020@gmail.com` / `littledoves`) — the first real school onboarded to live Finance. Created via a one-off `tinker` script (not the UI, since the incident was still being chased down at the time) using the exact same `SchoolProvisioningService::provisionSchool()` code path the UI calls, so it's provisioned identically to how the UI would have done it, not a special case.

## Resolved (security): Academics owner_routes.php had zero role enforcement — any logged-in user could reach the full Owner dashboard (2026-07-31)

First finding of the Academics testing pass (announcements/notes/support tickets/portals, per standing plan to test Academics after Finance). Static read of `routes/owner_routes.php` showed every `/owner/*` route guarded only by `InitializeTenantDatabase`/`LogRequests` — no role check anywhere, unlike `school_admin_routes.php` (`tenant.role:Admin`) or `super_admin_routes.php`/`system_admin_routes.php` (`role:super_admin`/`role:system_admin`). Confirmed live, not just theoretical: logged in as the **Student** test account (lowest-privilege role in the system) and navigated directly to `/owner/dashboard` — got the full Owner Dashboard with real business/financial data (staff breakdown, collection rate, revenue) and Quick Action links to Manage Students/Manage Staff/SMS. Same result logged in as School Admin.

**Fix required more care than "just add `tenant.role:Owner`"** — several of the seven `/owner/*` route groups in this one file are genuinely shared across roles despite the URL prefix:
- `owner/classes` — also linked from `dashboard/headmaster.blade.php` (Headmaster manages classes too)
- `owner/students` — also linked from `dashboard/accountant.blade.php` (Academics has its own Accountant role, separate from Finance's)
- `owner/attendance` — the whole block (including a route literally named `teacher.submitAttendance`) is called from `content/dashboard/classTeacher/teacher_attendance_type.blade.php` and other Teacher/ClassTeacher views — **left this block untouched**, restricting it would have broken real attendance-taking for teachers.

Verified the correct role set for each group by grepping every view folder for `route('<name>'` references before touching anything (a first attempt that blanket-restricted all seven groups to `Owner` was caught and reverted before deploying). Verified live post-fix: Student and Admin both now get a clean `403 Forbidden` on `/owner/dashboard`; ClassTeacher still reaches `/owner/attendance`.

**Same gap confirmed in `headmaster_routes.php` and `teacher_routes.php` too** (both had route groups but zero `tenant.role:` checks, matching the exact pattern Finance's tenant-connection bug and `SubstituteBindings` bug both showed — recurring across similarly-structured files rather than being a one-off). Confirmed live: Student could reach `/headmaster/` dashboard with full data. Fixed with the same grep-first methodology: `/headmaster` + `/headmaster/students` → `Headmaster`; `/academic` + `/academic/students` → `Academic`; `/accountant` → `Accountant`; `/teachers` + `/teachers/students` + `/exams` → `Teacher,ClassTeacher`; `/class-teacher/report-cards` → `ClassTeacher`. Left Assignments, the AssignmentController-based Notes, Support, Calendar, SMS, Suggestions untouched — confirmed via the same grep that multiple roles legitimately use them.

**The grep-based `route('<name>')` methodology has one real blind spot: hardcoded `href="/owner/..."` strings don't show up in a `route()`-name search.** Found this the hard way: `school_admin/dashboard.blade.php` links directly to `/owner/students`, `/owner/classes`, `/owner/subjects`, `/owner/promotions`, `/owner/grades`, and `/owner/passwords` via raw hrefs, not `route()` helpers — invisible to the audit, so the first owner_routes.php deploy 403'd School Admin on their own dashboard's quick-action links. Fixed by adding `Admin` to each affected group (split `owner.promotions.*` into its own group since Admin needs that but not the pure staff-management routes it used to share a group with). **If auditing another route file this way, grep for hardcoded `href="/prefix/` strings too, not just `route('name')` calls** — this is now the standard method, documented here so it isn't relearned.

Final role sets, all verified live (correct role can still reach it, wrong role gets 403): `owner.dashboard`/staff/staff-promotion → `Owner`; `owner.promotions.*` → `Owner,Admin`; `owner.grades.*` → `Owner,Admin`; `owner/classes` → `Owner,Admin,Headmaster`; `owner/students` → `Owner,Admin,Accountant`; `owner/subjects` → `Owner,Admin`; `owner/passwords` → `Owner,Admin`; `owner/attendance` → unchanged (shared). Deployed to both sandbox and live (Academics live currently has zero real schools, so no real-world exposure window, but worth having closed before the first one onboards).

## Academics testing pass: Announcements, Teacher Notes, Support Tickets, Student portal (2026-07-31)

Continuing the standing plan (test Academics after Finance) once the role-enforcement fix above was deployed. All tested with real data as the actual role (Academic/ClassTeacher/Admin/Student), not just read from code.

**Announcements — works correctly end-to-end.** Create → view → download all verified as real users. One real but low-severity bug found and fixed: the JSON/mobile API response in `AnnouncementController` (`index()` and `download()`) built the download URL as `/announcements/download/{id}`, which doesn't match the actual registered route (`/students/download/{id}`) — would 404 for any client following it. The web UI's own `<a href>` was already correct (uses the right route name), so this only affected the API response field, not real usage through the browser. Fixed both occurrences.

**Teacher Notes (`TeacherController::addNotes`) — confirmed real defect, low current impact.** The upload form only has Description + File fields — no class/subject selector — and `addNotes()` never sets `class_id`/`subject_id` even though `teacher_notes` has both columns. Confirmed via direct DB query after a real upload: both landed `NULL`. However, no currently-reachable UI (Teacher/ClassTeacher/Academic/Headmaster's own "My Documents" list, or any Student-facing route) actually filters notes by class/subject right now, so this has zero visible impact today — it would only bite if a class-filtered notes view is ever added. Documented, not fixed (would need a product decision on whether/how notes should be scoped per class).

**Support Tickets — creation/comments/admin-visibility all work; escalation beyond the first level is broken.** Verified: a ClassTeacher-created ticket is visible and manageable by School Admin (status/priority/assignment/comments all functional). But escalating a ticket fails silently past the first level — confirmed via direct DB check that `escalated_to`/`escalated_at` never get set. Root cause: `SupportTicketController::findAdminForLevel()`'s fallback branch (for `system_admin`/`super_admin` levels) queries the **tenant** `SchoolUser` table — but System Admin and Super Admin are platform/central-level accounts that never exist as tenant users, so the query always returns null and `escalateTicket()` bails out with "No admin available at next escalation level." The code's own comment already admits this ("these are in main database... for now, return first available admin" — the fallback was never actually updated to do that). Not fixed — needs an architectural decision on how to look up central System/Super Admin accounts from tenant context (likely analogous to Finance's `PlatformSuperAdmin` cross-database pattern), left as a known gap the same way Bank Integration's incomplete auto-match was.

**Student portal — all core sections verified working with real data:** Results (real Midterm Exam marks displayed correctly, though the per-subject Grade column shows "N/A" even when the Overall Grade correctly computes "A" — minor display inconsistency, not investigated further), Attendance (real history + percentage), Assignments (correct empty state), Notes (correct empty state), Payments (`parent.payments.*`, correct empty state — this is genuinely "view your own fees under the Student session," not a real separate Parent identity, confirmed during the original code survey).

## Resolved: Support Ticket escalation fixed; two stale legacy Finance links found in Academics menus (2026-08-06)

**Support Ticket escalation, fixed.** `SupportTicketController::findAdminForLevel()`'s fallback for `system_admin`/`super_admin` levels was querying the **tenant** `SchoolUser` table — but System Admin and Super Admin are central/platform accounts (`App\Models\User`, default connection), never tenant rows, so the query always returned null and every escalation past the first level silently failed (confirmed earlier this session via direct DB check: `escalated_to`/`escalated_at` stayed NULL after a real escalation attempt). Fixed by mirroring the lookup `SystemAdminController::dashboard()` already does in the reverse direction: `system_admin` → look up the current school via `session('school_code')`, then `SchoolAdmin::where('school_id', ...)->where('admin_type', 'system_admin')->where('is_active', true)->first()->admin`; `super_admin` → first `User` whose `userRole` is `'super_admin'` (platform-wide, no per-school assignment exists for that level). **Not yet deployed or re-verified live** — written and syntax-checked (`php -l` clean) while the host was in one of its repeated whole-server outages this session (SSH refusing all commands, HTTPS ECONNREFUSED on every domain, Finance included — confirmed via WebFetch, not just SSH). **First thing to do in a new session: deploy this commit (already pushed to both `sandbox` and `main`, commit `278b342`) and re-run the escalation test** (login as School Admin `S0011001`/`TestPass2026!` on academics-sandbox, open ticket TKT-2026-0001 at `/support/1`, use the escalate form, then check `support_tickets.escalated_to` is non-null and the target admin actually appears assigned).

**Two stale legacy links found and fixed while investigating a user-reported "Finance login broken" report that turned out to be unrelated to this session's work.** Academics' Student and Headmaster sidebar menus (`resources/menu/verticalMenuStudent.json`, `verticalMenuHeadTeacher.json`) hardcoded a "Finance Portal" link to `finance.olamtec.co.tz` — an **old, separate, unmaintained standalone Finance codebase** (see memory: `ERP_finance` repo / local `darasa-finance` folder), not anything in the darasa360-v3 monorepo this project has been working on. That old domain's DB credentials are broken (`Access denied for user 'olamtecc_darasa_user'@'localhost' ... Database: olamtecc_darasa_finance`) — a pre-existing, unrelated problem on a system this project doesn't maintain, surfaced only because a user clicked the stale link. Updated both to point to `finance.darasa360.co.tz` (the real, actively-maintained Finance app) instead. **Also not yet deployed to servers** (same outage) — same commit `278b342`.

## Credentials & server access
See Claude memory (`project_darasa_finance.md`) for: SSH key setup, DB credentials (sandbox + live), the scoped DirectAdmin API login key used by the apps themselves, and login credentials for each portal. Not duplicated here.
